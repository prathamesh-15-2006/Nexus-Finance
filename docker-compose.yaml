services:
  backend:
    build: ./Nexus-Backend
    container_name: backend
    ports:
      - "5000:5000"

  frontend:
    build: ./Nexus-Frontend
    container_name: frontend
    ports:
      - "3000:80"