version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      MONGO_URI: your_uri
      JWT_SECRET: secret

  frontend:
    build: ./frontend
    ports:
      - "3000:80"